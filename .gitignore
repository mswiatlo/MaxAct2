# Xcode build output
build/
DerivedData/
*.xcworkspace/xcuserdata/
*.xcodeproj/xcuserdata/
*.xcodeproj/project.xcworkspace/xcuserdata/

# Swift Package Manager
.build/
.swiftpm/
Package.resolved

# macOS
.DS_Store

# Python bytecode from the spike scripts.
__pycache__/
*.pyc

# Spike captures now live outside the repo entirely (~/.maxact-spike-captures), because they hold
# real GPS traces and heart rate. This rule stays as a guard against them reappearing here.
Spikes/captures/
